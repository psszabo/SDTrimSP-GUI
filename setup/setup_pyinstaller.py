# Since py2exe didn't work, pyinstaller was used
# From inside the main folder, call
#   pyinstaller main.py --icon=icons/tu_logo.ico --name=SDTrimSP-GUI --onefile --windowed --clean
#
# # REMOVE ANY "build" or "dist" folders before running this!